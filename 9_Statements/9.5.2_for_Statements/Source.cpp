#include <iostream>
#include <string>
using namespace std;


void f(int v[], int max)
{
	for (int i = 0; i != max; ++i)
		v[i] = i * i;
}

for (string s; cin >> s;) v.push_back(s);

for (;;) {  // forever
	// ...
}